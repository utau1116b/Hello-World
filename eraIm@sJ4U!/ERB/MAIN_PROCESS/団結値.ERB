@’cŒ‹’lã¸(ARG)
FOR LOCAL, 1080, 1085
	IF FLAG:LOCAL > 0
		BASE:(FLAG:LOCAL):’cŒ‹’l += ARG
	ENDIF
NEXT

@SUM_’cŒ‹’l()
#FUNCTION
#LOCALSIZE 4
VARSET LOCAL
FOR LOCAL, 1080, 1085
	IF FLAG:LOCAL > 0
		LOCAL:1 = FLAG:LOCAL
		LOCAL:2 += BASE:(LOCAL:1):’cŒ‹’l
		LOCAL:3 ++
	ENDIF
NEXT
LOCAL:2 = LOCAL:2 * 100 / LOCAL:3 / 100
RETURNF LOCAL:2

@ABR_’cŒ‹’l()
#FUNCTION
#LOCALSIZE 3
VARSET LOCAL
FOR LOCAL, 1080, 1085
	IF FLAG:LOCAL > 0
		LOCAL:1 += BASE:(FLAG:LOCAL):’cŒ‹’l
		LOCAL:2 ++
	ENDIF
NEXT
LOCAL:1 /= LOCAL:2
RETURNF LOCAL:1
