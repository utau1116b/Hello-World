@äàìÆê¨â (ARG,ARG:1,ARG:2,ARG:3,ARG:4,ARG:5,ARG:6,ARG:7,ARG:8,ARG:9,ARG:10,ARG:11,ARG:12,ARG:13,ARG:14,ARG:15,ARG:16,ARG:17,ARG:18,ARG:19)
#DIM PREV_MNY_G, 20
#DIM NOW_MNY_G, 20
#DIM PREV_MNY
#DIM NOW_MNY
#DIM éûä‘
VARSET LOCAL
VARSET LOCALS
VARSET PREV_MNY_G, 0
VARSET NOW_MNY_G, 0
VARSET PREV_MNY, 0
VARSET NOW_MNY, 0
VARSET éûä‘, 0
PREV_MNY = 0
;PRINTFORML {ARG:19}
FOR LOCAL, 0, 20
	IF ARG:LOCAL != 0
		SELECTCASE LOCAL
			CASE 0
				LOCALS = CDÇÃîÑÇËè„Ç∞
			CASE 10
				LOCALS:10 = éÿã‡ÇÃï‘çœ
			CASE 19
				LOCALS:19 = ããóøÇÃéxï•Ç¢
		ENDSELECT
		PREV_MNY_G:LOCAL += ARG:LOCAL
	ENDIF
NEXT
FOR LOCAL, 0, 20
	SIF LOCALS:LOCAL != ""
		NOW_MNY += PREV_MNY_G:LOCAL
NEXT

éûä‘ = 32

DO
	REDRAW 0
	CALL SAVELINE
	ALIGNMENT LEFT
	IF LOCAL:1 == éûä‘
		FOR LOCAL, 0, 20
			LOCAL:(LOCAL+20) = NOW_MNY_G:LOCAL
		NEXT
		LOCAL:40 = NOW_MNY
	ELSE
		FOR LOCAL, 0, 20
			LOCAL:(LOCAL+20) = (NOW_MNY_G:LOCAL*LOCAL:1+PREV_MNY_G:LOCAL*(éûä‘-LOCAL:1))/éûä‘

		NEXT
		LOCAL:40 = (NOW_MNY * LOCAL:1 + PREV_MNY * (éûä‘ - LOCAL:1)) / éûä‘
	ENDIF
	CUSTOMDRAWLINE =
	CALL PRINTCOLORL(0XEA70BF, "Ç®édéñåãâ ")
	DRAWLINE
	PRINTL
	FOR LOCAL, 0, 20
		SIF LOCALS:LOCAL != ""
			CALL ê¨â ï\é¶_ÉAÉjÉÅ(@"%LOCALS:LOCAL%",0,LOCAL:(LOCAL+20))
	NEXT
	LOCALS:20 = \@ LOCAL:40 < 0 ? - # + \@{LOCAL:40}
	PRINTFORML çáåv %LOCALS:20, 22%
	DRAWLINE
	;åvéZé¿çsëOÇÃämîF
	SIF LOCAL:1 == 0
		WAIT
	SIF LOCAL:1 == éûä‘
		BREAK
	TINPUT 100, -999, 0
	IF RESULT == -999
		LOCAL:1++
		REDRAW 1
		CALL RETURNLINE
		CONTINUE
	ELSE
		LOCAL:1 = éûä‘
		CONTINUE
	ENDIF
LOOP 1
PRINTW

@ê¨â ï\é¶_ÉAÉjÉÅ(ARGS, ARG, ARG:1)
VARSET LOCALS
PRINTFORM %ARGS, 20,LEFT%ÅF
LOCALS += \@ ARG:1 < 0 ?  # + \@
LOCALS:1 = {ARG:1, 6, LEFT}
LOCALS += LOCALS:1
IF ARG:1 < 0
	CALL PRINTCOLOR(0X8eace2, LOCALS)
ELSE
	CALL PRINTCOLOR(0XEA70BF, LOCALS)
ENDIF
PRINTL 
PRINTFORML ÅPÅPÅPÅPÅPÅPÅPÅPÅPÅPÅPÅPÅPÅPÅP

@ÉtÉ@ÉìÇ©ÇÁÇÃé˚âv
#FUNCTION
[SKIPSTART]
VARSET LOCAL
LOCAL:1 = çwîÉóÕ("ñkìå", FLAG:ñkìåíçñ⁄ìx)
LOCAL:2 = çwîÉóÕ("éÒìs", FLAG:éÒìsíçñ⁄ìx)
LOCAL:3 = çwîÉóÕ("íÜâõ", FLAG:íÜâõíçñ⁄ìx)
LOCAL:4 = çwîÉóÕ("è„ï˚", FLAG:è„ï˚íçñ⁄ìx)
LOCAL:5 = çwîÉóÕ("êº", FLAG:êºíçñ⁄ìx)
LOCAL:6 = çwîÉóÕ("ìÏ", FLAG:ìÏíçñ⁄ìx)
;çwîÉóÕéZèo
LOCAL:7 = SUMARRAY(LOCAL, 1, 7)
LOCAL:7 /= 3
LOCAL:7 = LOCAL:7*(100+SUM_ABL("éññ±ãZî\"))/100
[SKIPEND]
LOCAL:7 = 20000
RETURNF LOCAL:7
