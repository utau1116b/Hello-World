@SHOW_TRAIN_ITEM_LIST
PRINTFORM 所持アイテム：
FOR LOCAL:0, 0, 100
	IF ITEM:(LOCAL:0) > 0
		PRINTFORM %ITEMNAME:(LOCAL:0)%
			IF ITEM:(LOCAL:0) >= 2
				PRINTFORM ×{ITEM:(LOCAL:0)}
				;LOCAL:1 += 1
				;IF (LOCAL:1 % 5) == 1
				;	PRINTL 
				;	LOCAL:1 = 0
				;ENDIF
			ENDIF
		PRINTFORM  
	ENDIF
NEXT
PRINTL
PRINTBUTTON "[戻る]", 0
PRINTL
$INPUT_LOOP
INPUT
IF RESULT == 0
	RETURN
ELSE
	REUSELASTLINE 入力値が不正です
	GOTO INPUT_LOOP
ENDIF
RESTART
