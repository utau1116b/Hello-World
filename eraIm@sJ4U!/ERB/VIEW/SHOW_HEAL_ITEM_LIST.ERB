
;回復アイテム一覧表示
@SHOW_HEAL_ITEM_LIST
;LOCALリスト
;0	ループ用
;1～所持フラグ
;50	一時記憶用
;51	一時記憶用
;53	キャラ番号保存
;初期化
FOR LOCAL:0, 1, 25
	LOCAL:(LOCAL:0) = 0
NEXT


DRAWLINE
PRINTFORML 使用するアイテムを選んでください　　回復アイテム
DRAWLINE

IF ITEM:スタミナドリンク > 0
	LOCAL:1 = 1
	PRINTFORM [ 1] %ITEMNAME:101, 26, LEFT%  {ITEM:スタミナドリンク,2}/15個   一人の
	CALL STARMARK_Y
	PRINTFORML を30回復する
ELSEIF FLAG:3601 > 0
	SETCOLOR 0x333333
	PRINTFORM [ 1] %ITEMNAME:101, 26, LEFT%  {ITEM:スタミナドリンク,2}/15個   一人の
	CALL STARMARK_Y
	PRINTFORML を30回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [ 1] －－－
	RESETCOLOR
ENDIF
IF ITEM:ミドルスタミナドリンク > 0
	LOCAL:2 = 1
	PRINTFORM [ 2] %ITEMNAME:102, 26, LEFT%  {ITEM:ミドルスタミナドリンク,2}/15個   一人の
	CALL STARMARK_Y
	PRINTFORML を100回復する
ELSEIF FLAG:3602 > 0
	SETCOLOR 0x333333
	PRINTFORM [ 2] %ITEMNAME:102, 26, LEFT%  {ITEM:ミドルスタミナドリンク,2}/15個   一人の
	CALL STARMARK_Y
	PRINTFORML を100回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [ 2] －－－
	RESETCOLOR
ENDIF
IF ITEM:ハイスタミナドリンク > 0
	LOCAL:3 = 1
	PRINTFORM [ 3] %ITEMNAME:103, 26, LEFT%  {ITEM:ハイスタミナドリンク,2}/15個   一人の
	CALL STARMARK_Y
	PRINTFORML を200回復する
ELSEIF FLAG:3603 > 0
	SETCOLOR 0x333333
	PRINTFORM [ 3] %ITEMNAME:103, 26, LEFT%  {ITEM:ハイスタミナドリンク,2}/15個   一人の
	CALL STARMARK_Y
	PRINTFORML を200回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [ 3] －－－
	RESETCOLOR
ENDIF
IF ITEM:エクススタミナドリンク > 0
	LOCAL:4 = 1
	PRINTFORM [ 4] %ITEMNAME:104, 26, LEFT%  {ITEM:エクススタミナドリンク,2}/15個   一人の
	CALL STARMARK_Y
	PRINTFORML を完全回復する
ELSEIF FLAG:3604 > 0
	SETCOLOR 0x333333
	PRINTFORM [ 4] %ITEMNAME:104, 26, LEFT%  {ITEM:エクススタミナドリンク,2}/15個   一人の
	CALL STARMARK_Y
	PRINTFORML を完全回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [ 4] －－－
	RESETCOLOR
ENDIF
IF ITEM:ソーマドリンク > 0
	LOCAL:5 = 1
	PRINTFORM [ 5] %ITEMNAME:105, 26, LEFT%  {ITEM:ソーマドリンク,2}/15個   全員の
	CALL STARMARK_Y
	PRINTFORML を50回復する
ELSEIF FLAG:3605 > 0
	SETCOLOR 0x333333
	PRINTFORM [ 5] %ITEMNAME:105, 26, LEFT%  {ITEM:ソーマドリンク,2}/15個   全員の
	CALL STARMARK_Y
	PRINTFORML を50回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [ 5] －－－
	RESETCOLOR
ENDIF
IF ITEM:ハイソーマドリンク > 0
	LOCAL:6 = 1
	PRINTFORM [ 6] %ITEMNAME:106, 26, LEFT%  {ITEM:ハイソーマドリンク,2}/15個   全員の
	CALL STARMARK_Y
	PRINTFORML を150回復する
ELSEIF FLAG:3603 > 0
	SETCOLOR 0x333333
	PRINTFORM [ 6] %ITEMNAME:106, 26, LEFT%  {ITEM:ハイソーマドリンク,2}/15個   全員の
	CALL STARMARK_Y
	PRINTFORML を150回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [ 6] －－－
	RESETCOLOR
ENDIF
IF ITEM:エクスソーマドリンク > 0
	LOCAL:7 = 1
	PRINTFORM [ 7] %ITEMNAME:107, 26, LEFT%  {ITEM:エクスソーマドリンク,2}/15個   全員の
	CALL STARMARK_Y
	PRINTFORML を完全回復する
ELSEIF FLAG:3607 > 0
	SETCOLOR 0x333333
	PRINTFORM [ 7] %ITEMNAME:107, 26, LEFT%  {ITEM:エクスソーマドリンク,2}/15個   全員の
	CALL STARMARK_Y
	PRINTFORML を完全回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [ 7] －－－
	RESETCOLOR
ENDIF
IF ITEM:エナジードリンク > 0
	LOCAL:8 = 1
	PRINTFORM [ 8] %ITEMNAME:110, 26, LEFT%  {ITEM:エナジードリンク,2}/15個   1人の
	CALL HEARTMARK_P
	PRINTFORML を15回復する
ELSEIF FLAG:3610 > 0
	SETCOLOR 0x333333
	PRINTFORM [ 8] %ITEMNAME:110, 26, LEFT%  {ITEM:エナジードリンク,2}/15個   1人の
	CALL HEARTMARK_P
	PRINTFORML を15回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [ 8] －－－
	RESETCOLOR
ENDIF
IF ITEM:ミドルエナジードリンク > 0
	LOCAL:9 = 1
	PRINTFORM [ 9] %ITEMNAME:111, 26, LEFT%  {ITEM:ミドルエナジードリンク,2}/15個   1人の
	CALL HEARTMARK_P
	PRINTFORML を50回復する
ELSEIF FLAG:3611 > 0
	SETCOLOR 0x333333
	PRINTFORM [ 9] %ITEMNAME:111, 26, LEFT%  {ITEM:ミドルエナジードリンク,2}/15個   1人の
	CALL HEARTMARK_P
	PRINTFORML を50回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [ 9] －－－
	RESETCOLOR
ENDIF
IF ITEM:ハイエナジードリンク > 0
	LOCAL:10 = 1
	PRINTFORM [10] %ITEMNAME:112, 26, LEFT%  {ITEM:ハイエナジードリンク,2}/15個   1人の
	CALL HEARTMARK_P
	PRINTFORML を100回復する
ELSEIF FLAG:3612 > 0
	SETCOLOR 0x333333
	PRINTFORM [10] %ITEMNAME:112, 26, LEFT%  {ITEM:ハイエナジードリンク,2}/15個   1人の
	CALL HEARTMARK_P
	PRINTFORML を100回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [10] －－－
	RESETCOLOR
ENDIF
IF ITEM:エクスエナジードリンク > 0
	LOCAL:11 = 1
	PRINTFORM [11] %ITEMNAME:113, 26, LEFT%  {ITEM:エクスエナジードリンク,2}/15個   1人の
	CALL HEARTMARK_P
	PRINTFORML を完全回復する
ELSEIF FLAG:3613 > 0
	SETCOLOR 0x333333
	PRINTFORM [11] %ITEMNAME:113, 26, LEFT%  {ITEM:エクスエナジードリンク,2}/15個   1人の
	CALL HEARTMARK_P
	PRINTFORML を完全回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [11] －－－
	RESETCOLOR
ENDIF
IF ITEM:テキーラ > 0
	LOCAL:12 = 1
	PRINTFORML [12] %ITEMNAME:115, 26, LEFT%  {ITEM:テキーラ,2}/15個   1人のVLを10上昇させる
ELSEIF FLAG:3615 > 0
	SETCOLOR 0x333333
	PRINTFORML [12] %ITEMNAME:115, 26, LEFT%  {ITEM:テキーラ,2}/15個   1人のVLを10上昇させる
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [12] －－－
	RESETCOLOR
ENDIF
IF ITEM:スピリタス > 0
	LOCAL:13 = 1
	PRINTFORML [13] %ITEMNAME:116, 26, LEFT%  {ITEM:スピリタス,2}/15個   1人のVLを30上昇させる
ELSEIF FLAG:3616 > 0
	SETCOLOR 0x333333
	PRINTFORML [13] %ITEMNAME:116, 26, LEFT%  {ITEM:スピリタス,2}/15個   1人のVLを30上昇させる
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [13] －－－
	RESETCOLOR
ENDIF
IF ITEM:オレンジジュース > 0
	LOCAL:14 = 1
	PRINTFORML [14] %ITEMNAME:120, 26, LEFT%  {ITEM:オレンジジュース,2}/15個   1人の状態異常を回復する
ELSEIF FLAG:3620 > 0
	SETCOLOR 0x333333
	PRINTFORML [14] %ITEMNAME:120, 26, LEFT%  {ITEM:オレンジジュース,2}/15個   1人の状態異常を回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [14] －－－
	RESETCOLOR
ENDIF
IF ITEM:ゴールデンオレンジジュース > 0
	LOCAL:15 = 1
	PRINTFORML [15] %ITEMNAME:121, 26, LEFT%  {ITEM:ゴールデンオレンジジュース,2}/15個   全員の状態異常を回復する
ELSEIF FLAG:3621 > 0
	SETCOLOR 0x333333
	PRINTFORML [15] %ITEMNAME:121, 26, LEFT%  {ITEM:ゴールデンオレンジジュース,2}/15個   全員の状態異常を回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [15] －－－
	RESETCOLOR
ENDIF
IF ITEM:アウェイクンドリンク > 0
	LOCAL:16 = 1
	PRINTFORM [16] %ITEMNAME:123, 26, LEFT%  {ITEM:アウェイクンドリンク,2}/15個   1人の気絶を
	CALL STARMARK_Y
	PRINTFORML 20で回復する
ELSEIF FLAG:3623 > 0
	SETCOLOR 0x333333
	PRINTFORM [16] %ITEMNAME:123, 26, LEFT%  {ITEM:アウェイクンドリンク,2}/15個   1人の気絶を
	CALL STARMARK_Y
	PRINTFORML 20で回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [16] －－－
	RESETCOLOR
ENDIF
IF ITEM:ラストアウェイクンドリンク > 0
	LOCAL:17 = 1
	PRINTFORM [17] %ITEMNAME:124, 26, LEFT%  {ITEM:ラストアウェイクンドリンク,2}/15個   1人の気絶を
	CALL STARMARK_Y
	PRINTFORML 全回復で回復する
ELSEIF FLAG:3624 > 0
	SETCOLOR 0x333333
	PRINTFORM [17] %ITEMNAME:124, 26, LEFT%  {ITEM:ラストアウェイクンドリンク,2}/15個   1人の気絶を
	CALL STARMARK_Y
	PRINTFORML 全回復で回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [17] －－－
	RESETCOLOR
ENDIF
IF ITEM:スペシフィックⅠ > 0
	LOCAL:18 = 1
	PRINTFORM [18] %ITEMNAME:125, 26, LEFT%  {ITEM:スペシフィックⅠ,2}/15個   1人の
	CALL STARMARK_Y
	PRINTFORM を200、
	CALL HEARTMARK_P
	PRINTFORML を30回復する
ELSEIF FLAG:3525 > 0
	SETCOLOR 0x333333
	PRINTFORM [18] %ITEMNAME:125, 26, LEFT%  {ITEM:スペシフィックⅠ,2}/15個   1人の
	CALL STARMARK_Y
	PRINTFORM を200、
	CALL HEARTMARK_P
	PRINTFORML を30回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [18] －－－
	RESETCOLOR
ENDIF
IF ITEM:スペシフィックⅡ > 0
	LOCAL:19 = 1
	PRINTFORM [19] %ITEMNAME:126, 26, LEFT%  {ITEM:スペシフィックⅡ,2}/15個   1人の
	CALL STARMARK_Y
	PRINTFORM を300、
	CALL HEARTMARK_P
	PRINTFORML を50回復する
ELSEIF FLAG:3626 > 0
	SETCOLOR 0x333333
	PRINTFORM [19] %ITEMNAME:126, 26, LEFT%  {ITEM:スペシフィックⅡ,2}/15個   1人の
	CALL STARMARK_Y
	PRINTFORM を300、
	CALL HEARTMARK_P
	PRINTFORML を50回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [19] －－－
	RESETCOLOR
ENDIF
IF ITEM:スペシフィックⅢ > 0
	LOCAL:20 = 1
	PRINTFORM [20] %ITEMNAME:127, 26, LEFT%  {ITEM:スペシフィックⅢ,2}/15個   1人の
	CALL STARMARK_Y
	PRINTFORM 、
	CALL HEARTMARK_P
	PRINTFORML を完全回復する
ELSEIF FLAG:3627 > 0
	SETCOLOR 0x333333
	PRINTFORM [20] %ITEMNAME:127, 26, LEFT%  {ITEM:スペシフィックⅢ,2}/15個   1人の
	CALL STARMARK_Y
	PRINTFORM 、
	CALL HEARTMARK_P
	PRINTFORML を完全回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [20] －－－
	RESETCOLOR
ENDIF
IF ITEM:エリキシル剤 > 0
	LOCAL:21 = 1
	PRINTFORM [21] %ITEMNAME:128, 26, LEFT%  {ITEM:エリキシル剤,2}/15個   1人の
	CALL STARMARK_Y
	PRINTFORM 、
	CALL HEARTMARK_P
	PRINTFORML 、状態異常、気絶を完全回復する
ELSEIF FLAG:3628 > 0
	SETCOLOR 0x333333
	PRINTFORM [21] %ITEMNAME:128, 26, LEFT%  {ITEM:エリキシル剤,2}/15個   1人の
	CALL STARMARK_Y
	PRINTFORM 、
	CALL HEARTMARK_P
	PRINTFORML 、状態異常、気絶を完全回復する
	RESETCOLOR
ELSE
	SETCOLOR 0x333333
	PRINTFORML [21] －－－
	RESETCOLOR
ENDIF
PRINTL [ 0] 戻る


$INPUT_LOOP
INPUT

IF RESULT == 0
	RETURN
ELSEIF (RESULT >= 1) && (RESULT <= 21) && (LOCAL:RESULT > 0)
	;RESULT退避
	LOCAL:50 = RESULT
	IF (LOCAL:50 < 5) || (LOCAL:50 > 7 && LOCAL:50 < 15) || (LOCAL:50 > 15)
		;対象を取るアイテムの場合、対象指定
		PRINTFORML 誰に使いますか？
		;PTメンバー表示
		FOR LOCAL:0, 80, 85
			IF FLAG:(LOCAL:0 + 1000) >= 0
				PRINTFORM [{LOCAL:0 - 79}] %CALLNAME:(FLAG:(LOCAL:0 + 1000)),10,LEFT% 
				CALL STARMARK_Y
				PRINTFORM {BASE:(FLAG:(LOCAL:0 + 1000)):HP,6}/{MAXBASE:(FLAG:(LOCAL:0 + 1000)):HP,6} 
				CALL HEARTMARK_P
				PRINTFORM {BASE:(FLAG:(LOCAL:0 + 1000)):MP,3}/{MAXBASE:(FLAG:(LOCAL:0 + 1000)):MP,3} 
				PRINTFORM VL {BASE:(FLAG:(LOCAL:0 + 1000)):ボルテージ,3}/{MAXBASE:(FLAG:(LOCAL:0 + 1000)):ボルテージ,3}
				PRINT  
				CALL SHOW_BAD_STATUS(FLAG:(LOCAL:0 + 1000))
				PRINTL 
			ENDIF
		NEXT
		PRINTL [0] キャンセル
		
		INPUT

		IF (RESULT < 1) || (RESULT > 5) || FLAG:(RESULT + 1079) <= 0
			RESTART
		ELSE
			RESULT = FLAG:(RESULT + 1079)
		ENDIF
	ENDIF
		;効果発動
	SELECTCASE LOCAL:50
		;HP回復アイテム
		CASE 1
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:スタミナドリンク -= 1
			LOCAL:51 = 30
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 + 1)
			BASE:RESULT:HP += LOCAL:51
			IF BASE:RESULT:HP > MAXBASE:RESULT:HP
				BASE:RESULT:HP = MAXBASE:RESULT:HP
			ENDIF
			PRINTFORM %CALLNAME:RESULT%の
			CALL STARMARK_Y
			PRINTFORMW が{LOCAL:51}回復した！
		CASE 2
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:ミドルスタミナドリンク -= 1
			LOCAL:51 = 100
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 * 8 + 10) / 10
			BASE:RESULT:HP += LOCAL:51
			IF BASE:RESULT:HP > MAXBASE:RESULT:HP
				BASE:RESULT:HP = MAXBASE:RESULT:HP
			ENDIF
			PRINTFORM %CALLNAME:RESULT%の
			CALL STARMARK_Y
			PRINTFORMW が{LOCAL:51}回復した！
		CASE 3
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:ハイスタミナドリンク -= 1
			LOCAL:51 = 200
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 * 6 + 10) / 10
			BASE:RESULT:HP += LOCAL:51
			IF BASE:RESULT:HP > MAXBASE:RESULT:HP
				BASE:RESULT:HP = MAXBASE:RESULT:HP
			ENDIF
			PRINTFORM %CALLNAME:RESULT%の
			CALL STARMARK_Y
			PRINTFORMW が{LOCAL:51}回復した！
		CASE 4
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:エクススタミナドリンク -= 1
			BASE:RESULT:HP = MAXBASE:RESULT:HP
			PRINTFORM %CALLNAME:RESULT%の
			CALL STARMARK_Y
			PRINTFORMW が完全に回復した！
			
		CASE 5
			ITEM:ソーマドリンク -= 1
			LOCAL:51 = 50
			FOR LOCAL:0, 80, 85
				IF (FLAG:(LOCAL:0 +1000) >= 0) && (CFLAG:(FLAG:(LOCAL:0 + 1000)):気絶状態 <= 0)
					BASE:(FLAG:(LOCAL:0 + 1000)):HP += 50
					IF BASE:(FLAG:(LOCAL:0 + 1000)):HP > MAXBASE:(FLAG:(LOCAL:0 + 1000)):HP
						BASE:(FLAG:(LOCAL:0 + 1000)):HP = MAXBASE:(FLAG:(LOCAL:0 + 1000)):HP
					ENDIF
				ENDIF
			NEXT
			PRINTFORM 全員の
			CALL STARMARK_Y
			PRINTFORMW が{LOCAL:51}回復した！
		CASE 6
			ITEM:ハイソーマドリンク -= 1
			LOCAL:51 = 150
			FOR LOCAL:0, 80, 85
				IF (FLAG:(LOCAL:0 +1000) >= 0) && (CFLAG:(FLAG:(LOCAL:0 + 1000)):気絶状態 <= 0)
					BASE:(FLAG:(LOCAL:0 + 1000)):HP += 150
					IF BASE:(FLAG:(LOCAL:0 + 1000)):HP > MAXBASE:(FLAG:(LOCAL:0 + 1000)):HP
						BASE:(FLAG:(LOCAL:0 + 1000)):HP = MAXBASE:(FLAG:(LOCAL:0 + 1000)):HP
					ENDIF
				ENDIF
			NEXT
			PRINTFORM 全員の
			CALL STARMARK_Y
			PRINTFORMW が{LOCAL:51}回復した！
		CASE 7
			ITEM:エクスソーマドリンク -= 1
			FOR LOCAL:0, 1080, 1085
				IF (FLAG:(LOCAL:0) >= 0) && (CFLAG:(FLAG:(LOCAL:0)):気絶状態 <= 0)
					BASE:(FLAG:(LOCAL:0)):HP = MAXBASE:(FLAG:(LOCAL:0)):HP
				ENDIF
			NEXT
			PRINTFORM 全員の
			CALL STARMARK_Y
			PRINTFORMW が完全に回復した！
		;ここからTP回復アイテム
		CASE 8
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:エナジードリンク -= 1
			LOCAL:51 = 15
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 + 1)
			BASE:RESULT:MP += LOCAL:51
			IF BASE:RESULT:MP > MAXBASE:RESULT:MP
				BASE:RESULT:MP = MAXBASE:RESULT:MP
			ENDIF
			PRINTFORM %CALLNAME:RESULT%の
			CALL HEARTMARK_P
			PRINTFORMW が{LOCAL:51}回復した！
		CASE 9
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:ミドルエナジードリンク -= 1
			LOCAL:51 = 50
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 * 8 + 10) / 10
			BASE:RESULT:MP += LOCAL:51
			IF BASE:RESULT:MP > MAXBASE:RESULT:MP
				BASE:RESULT:MP = MAXBASE:RESULT:MP
			ENDIF
			PRINTFORM %CALLNAME:RESULT%の
			CALL HEARTMARK_P
			PRINTFORMW が{LOCAL:51}回復した！
		CASE 10
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:ハイエナジードリンク -= 1
			LOCAL:51 = 100
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 * 6 + 10) / 10
			BASE:RESULT:MP += LOCAL:51
			IF BASE:RESULT:MP > MAXBASE:RESULT:MP
				BASE:RESULT:MP = MAXBASE:RESULT:MP
			ENDIF
			PRINTFORM %CALLNAME:RESULT%の
			CALL HEARTMARK_P
			PRINTFORMW が{LOCAL:51}回復した！
		CASE 11
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:エクスエナジードリンク -= 1
			BASE:RESULT:MP = MAXBASE:RESULT:MP
			PRINTFORM %CALLNAME:RESULT%の
			CALL HEARTMARK_P
			PRINTFORMW が完全に回復した！
		;ここからVL上昇系アイテム
		CASE 12
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:テキーラ -= 1
			LOCAL:51 = 10
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 + 1)
			BASE:RESULT:ボルテージ += LOCAL:51
			IF BASE:RESULT:ボルテージ > MAXBASE:RESULT:ボルテージ
				BASE:RESULT:ボルテージ = MAXBASE:RESULT:ボルテージ
			ENDIF
			PRINTFORMW %CALLNAME:RESULT%のVLが{LOCAL:51}上昇した！
		CASE 13
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:スピリタス -= 1
			LOCAL:51 = 30
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 * 8 + 10) / 10
			BASE:RESULT:ボルテージ += LOCAL:51
			IF BASE:RESULT:ボルテージ > MAXBASE:RESULT:ボルテージ
				BASE:RESULT:ボルテージ = MAXBASE:RESULT:ボルテージ
			ENDIF
			PRINTFORMW %CALLNAME:RESULT%のVLが{LOCAL:51}上昇した！
		;ここから状態異常回復アイテム
		CASE 14
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:オレンジジュース -= 1
			FOR LOCAL:0, 1050, 1067
				CFLAG:RESULT:(LOCAL:0) = 0
			NEXT
			PRINTFORMW %CALLNAME:RESULT%の状態異常が回復した！
			CALL STATUS_RENEW_2(RESULT)
		CASE 15
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:ゴールデンオレンジジュース -= 1
			FOR LOCAL:51, 1080, 1085
				FOR LOCAL:0, 1050, 1067
					CFLAG:(FLAG:(LOCAL:51)):(LOCAL:0) = 0
				NEXT
			NEXT
			PRINTFORMW 全員の状態異常が回復した！
			FOR LOCAL:51, 1080, 1085
				CALL STATUS_RENEW_2((FLAG:(LOCAL:51)))
			NEXT
		CASE 16
			IF CFLAG:RESULT:気絶状態 <= 0
				PRINTFORMW %CALLNAME:RESULT%は気絶していません
				RESTART
			ENDIF
			ITEM:アウェイクンドリンク -= 1
			LOCAL:53 = RESULT
			LOCAL:51 = 20
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 + 1)
			IF CFLAG:RESULT:気絶状態
				CFLAG:RESULT:気絶状態 = 0
			ENDIF
			BASE:RESULT:HP += LOCAL:51
			IF BASE:RESULT:HP > MAXBASE:RESULT:HP
				BASE:RESULT:HP = MAXBASE:RESULT:HP
			ENDIF
			;気絶後の状態を待機中にする
			IF CFLAG:RESULT:現在状態 < 0
				CFLAG:RESULT:現在状態 = 0
			ENDIF
			CALL STATUS_RENEW_2(RESULT)
			;BASE:RESULT:HP += LOCAL:51
			;IF BASE:RESULT:HP > MAXBASE:RESULT:HP
			;	BASE:RESULT:HP = MAXBASE:RESULT:HP
			;ENDIF
			PRINTFORMW %CALLNAME:(LOCAL:53)%は気絶から回復した！
		CASE 17
			IF CFLAG:RESULT:気絶状態 <= 0
				PRINTFORMW %CALLNAME:RESULT%は気絶していません
				RESTART
			ENDIF
			ITEM:ラストアウェイクンドリンク -= 1
			LOCAL: 53 = RESULT
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 * 8 + 10) / 10
			CFLAG:RESULT:気絶状態 = 0
			BASE:RESULT:HP = MAXBASE:RESULT:HP
			IF BASE:RESULT:HP > MAXBASE:RESULT:HP
				BASE:RESULT:HP = MAXBASE:RESULT:HP
			ENDIF
			;気絶後の状態を待機中にする
			IF CFLAG:RESULT:現在状態 < 0
				CFLAG:RESULT:現在状態 = 0
			ENDIF
			CALL STATUS_RENEW_2(RESULT)
			PRINTFORMW %CALLNAME:(LOCAL:53)%は気絶から回復した！
		CASE 18
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:スペシフィックⅠ -= 1
			LOCAL:51 = 200
			LOCAL:52 = 30
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 + 1)
			BASE:RESULT:HP += LOCAL:51
			IF BASE:RESULT:HP > MAXBASE:RESULT:HP
				BASE:RESULT:HP = MAXBASE:RESULT:HP
			ENDIF
			BASE:RESULT:MP += LOCAL:52
			IF BASE:RESULT:MP > MAXBASE:RESULT:MP
				BASE:RESULT:MP = MAXBASE:RESULT:MP
			ENDIF
			PRINTFORM %CALLNAME:RESULT%の
			CALL STARMARK_Y
			PRINTFORML が{LOCAL:51}回復した！
			PRINTFORM %CALLNAME:RESULT%の
			CALL HEARTMARK_P
			PRINTFORMW が{LOCAL:52}回復した！
		CASE 19
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:スペシフィックⅡ -= 1
			LOCAL:51 = 300
			LOCAL:52 = 50
			LOCAL:53 = RESULT
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 * 8 + 10) / 10
			BASE:RESULT:HP += LOCAL:51
			IF BASE:RESULT:HP > MAXBASE:RESULT:HP
				BASE:RESULT:HP = MAXBASE:RESULT:HP
			ENDIF
			BASE:RESULT:MP += LOCAL:52
			IF BASE:RESULT:MP > MAXBASE:RESULT:MP
				BASE:RESULT:MP = MAXBASE:RESULT:MP
			ENDIF
			PRINTFORM %CALLNAME:RESULT%の
			CALL STARMARK_Y
			PRINTFORML が{LOCAL:51}回復した！
			PRINTFORM %CALLNAME:RESULT%の
			CALL HEARTMARK_P
			PRINTFORMW が{LOCAL:52}回復した！
		CASE 20
			IF CFLAG:RESULT:気絶状態
				PRINTFORMW %CALLNAME:RESULT%は気絶しているので効果がありません
				RESTART
			ENDIF
			ITEM:スペシフィックⅢ -= 1
			;知識
			;LOCAL:51 = LOCAL:51 * (ABL:RESULT:231 * 6 + 10) / 10
			BASE:RESULT:HP = MAXBASE:RESULT:HP
			IF BASE:RESULT:HP > MAXBASE:RESULT:HP
				BASE:RESULT:HP = MAXBASE:RESULT:HP
			ENDIF
			BASE:RESULT:MP = MAXBASE:RESULT:MP
			IF BASE:RESULT:MP > MAXBASE:RESULT:MP
				BASE:RESULT:MP = MAXBASE:RESULT:MP
			ENDIF
			PRINTFORM %CALLNAME:RESULT%の
			CALL STARMARK_Y
			PRINTFORM と
			CALL HEARTMARK_P
			PRINTFORMW が完全に回復した！
		CASE 21
			ITEM:エリキシル剤 -= 1
			FOR LOCAL:0, 1050, 1068
				CFLAG:RESULT:(LOCAL:0) = 0
			NEXT
			;気絶後の状態を待機中にする
			IF CFLAG:RESULT:現在状態 < 0
				CFLAG:RESULT:現在状態 = 0
			ENDIF
			LOCAL:53 = RESULT
			BASE:RESULT:HP = MAXBASE:RESULT:HP
			IF BASE:RESULT:HP > MAXBASE:RESULT:HP
				BASE:RESULT:HP = MAXBASE:RESULT:HP
			ENDIF
			BASE:RESULT:MP = MAXBASE:RESULT:MP
			IF BASE:RESULT:MP > MAXBASE:RESULT:MP
				BASE:RESULT:MP = MAXBASE:RESULT:MP
			ENDIF
			CALL STATUS_RENEW_2(RESULT)
			PRINTFORMW %CALLNAME:(LOCAL:53)%は完全に回復した！
		CASEELSE
			PRINTFORMW IDエラー　不正なアイテムID{LOCAL:50}が指定されました
	ENDSELECT
	RETURN 1
ELSE
	REUSELASTLINE 入力値が不正です。再入力してください
	GOTO INPUT_LOOP
ENDIF

RESTART
