;ARG	参照する装備
;ARG:1	0比較しない 1～対象の装備中の服と比較
@装備性能(ARG, ARG:1)
VARSET LOCAL
DRAWLINE
IF ARG > 0
	PRINTFORM %ITEMNAME:ARG, 24, LEFT% 
	CALL 装備タイプ表示(ARG)
ELSE
	PRINTFORM %SAVESTR:199, 24, LEFT%
ENDIF
PRINTL 
CALL 装備用VOVIDABAR(ARG)
IF ARG:1 > 0
	PRINT  (
	FOR LOCAL, 0, 7
		SELECTCASE LOCAL
			CASE 0
				TSTR = HP
			CASE 1
				TSTR = MP
			CASE 2
				TSTR = POW
			CASE 3
				TSTR = GUA
			CASE 4
				TSTR = DEX
			CASE 5
				TSTR = SPD
			CASE 6
				TSTR = LUC
			CASEELSE
				TSTR = ""
		ENDSELECT
		TRYCALLFORM %TSTR%_{ARG}
		LOCAL:1 = RESULT
		IF EQUIP:(ARG:1):装着中服ID > 0
			TRYCALLFORM %TSTR%_{EQUIP:(ARG:1):装着中服ID}
			LOCAL:2 = RESULT
		ELSE
			LOCAL:2 = 0
		ENDIF
		IF LOCAL:2 < LOCAL:1
			;上回るなら水色
			SETCOLOR 0x10EEEE
			PRINT +
		ELSEIF LOCAL:2 == LOCAL:1
			;同じなら白
			RESETCOLOR
			PRINT +
		ELSE
			;下回るなら赤色
			SETCOLOR 0xEE1010
			PRINT -
		ENDIF
		PRINTFORM {ABS(LOCAL:1 - LOCAL:2) / 100, 3}
		RESETCOLOR
			IF LOCAL < 6
				PRINT )     (
			ELSE
				;最後は閉じるだけ
				PRINT )
			ENDIF
		NEXT
		PRINTL 
ENDIF
CALL SHOW_CLOTH_PARAM(ARG)
PRINTL
DRAWLINE
PRINTFORML 耐性
DRAWLINE
;ARG 衣装ID
@SHOW_CLOTH_PARAM(ARG)
CALL STARMARK_Y
PRINTFORM  
RESETCOLOR
;スキルがあれば、スキルの分も足す
TRYCALLFORM HP_{ARG}
PRINTFORM {RESULT  / 100, 3}
RESETCOLOR
PRINTFORM    / 
CALL HEARTMARK_P
PRINTFORM  
RESETCOLOR
TRYCALLFORM MP_{ARG}
PRINTFORM {RESULT  / 100, 3}
RESETCOLOR
PRINTFORM    / 
CALL SET_COLOR("春香")
PRINTFORM POW 
RESETCOLOR
TRYCALLFORM POW_{ARG}
PRINTFORM {RESULT  / 100, 3}
RESETCOLOR
PRINTFORM    / 
CALL SET_COLOR("真美")
PRINTFORM GUA 
RESETCOLOR
TRYCALLFORM GUA_{ARG}
PRINTFORM {RESULT  / 100, 3}
RESETCOLOR
PRINTFORM    / 
CALL SET_COLOR("律子")
PRINTFORM DEX 
RESETCOLOR
TRYCALLFORM DEX_{ARG}
PRINTFORM {RESULT  / 100, 3}
RESETCOLOR
PRINTFORM    / 
CALL SET_COLOR("響")
PRINTFORM SPD 
RESETCOLOR
TRYCALLFORM SPD_{ARG}
PRINTFORM {RESULT  / 100, 3}
RESETCOLOR
PRINTFORM    / 
CALL SET_COLOR("雪歩")
PRINTFORM LUC 
RESETCOLOR
TRYCALLFORM LUC_{ARG}
PRINTFORM {RESULT  / 100, 3}
RESETCOLOR

@装備タイプ表示(ARG)
TRYCALLFORM 装備タイプ_{ARG}
SELECTCASE RESULT
	CASE 1
		CALL SET_COLOR("ボーカル")
	CASE 2
		CALL SET_COLOR("ダンス")
	CASE 3
		CALL SET_COLOR("ビジュアル")
	CASEELSE
		RETURN
		;SETCOLOR 192, 192, 192
ENDSELECT
PRINTFORM ★
RESETCOLOR
;ARG	参照する装備
;ARG:1	0比較しない 1～対象の装備中の服と比較
;ARG:2	アクセサリータイプ
@装備性能アクセサリー(ARG, ARG:1)
VARSET LOCAL
DRAWLINE
SELECTCASE ARG
	;装備無はスルー
	CASE 0
		
	;HEAD
	CASE 1000 TO 1099
		LOCAL:3 = 113
	;BODY
	CASE 1100 TO 1199
		LOCAL:3 = 114
	;HAND
	CASE 1200 TO 1299
		LOCAL:3 = 115
	;LEG
	CASE 1300 TO 1399
		LOCAL:3 = 116
	;お守り
	CASE 1400 TO 1499
		LOCAL:3 = 117
	CASEELSE
		PRINTFORMW エラー！不明なケース{ARG}が選択されました(@装備性能アクセサリー)
ENDSELECT
IF ARG > 0
	PRINTFORM %ITEMNAME:ARG, 24, LEFT% 
;	CALL 装備タイプ表示(ARG)
ELSE
	PRINTFORM %SAVESTR:199, 24, LEFT%
ENDIF
PRINTL 
CALL 装備用VOVIDABAR(ARG)
IF ARG:1 > 0
	PRINT  (
	FOR LOCAL, 0, 7
		SELECTCASE LOCAL
			CASE 0
				TSTR = HP
			CASE 1
				TSTR = MP
			CASE 2
				TSTR = POW
			CASE 3
				TSTR = GUA
			CASE 4
				TSTR = DEX
			CASE 5
				TSTR = SPD
			CASE 6
				TSTR = LUC
			CASEELSE
				TSTR = ""
		ENDSELECT
		TRYCALLFORM %TSTR%_{ARG}
		LOCAL:1 = RESULT
		IF CFLAG:(ARG:1):(LOCAL:3)> 0
			TRYCALLFORM %TSTR%_{CFLAG:(ARG:1):(LOCAL:3)}
			LOCAL:2 = RESULT
		ELSE
			LOCAL:2 = 0
		ENDIF
		IF LOCAL:2 < LOCAL:1
			;上回るなら水色
			SETCOLOR 0x10EEEE
			PRINT +
		ELSEIF LOCAL:2 == LOCAL:1
			;同じなら白
			RESETCOLOR
			PRINT +
		ELSE
			;下回るなら赤色
			SETCOLOR 0xEE1010
			PRINT -
		ENDIF
		PRINTFORM {ABS(LOCAL:1 - LOCAL:2) / 100, 3}
		RESETCOLOR
			IF LOCAL < 6
				PRINT )     (
			ELSE
				;最後は閉じるだけ
				PRINT )
			ENDIF
		NEXT
		PRINTL 
ENDIF
CALL SHOW_CLOTH_PARAM(ARG)
PRINTL
DRAWLINE
PRINTFORML 耐性
DRAWLINE
