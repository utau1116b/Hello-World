@SHOW_BATTLE_ITEM_LIST(ARG:0)
$START
DRAWLINE
PRINTL 使用・確認するアイテムを選んでください
DRAWLINE
PRINTFORML [ 1] 回復系
PRINTFORML [ 2] 攻撃系
PRINTFORML [ 3] 補助系
PRINTFORML [ 0] 戻る

$INPUT_LOOP
INPUT

IF RESULT == 0
	RETURN
ELSEIF RESULT == 1
	CALL SHOW_HEAL_ITEM_LIST
	IF RESULT == 0
		;アイテム使用をキャンセルした場合、最初に戻す
		GOTO START
	ELSE
		;CALL SET_DEFAULT_WAIT(ARG:0)
		RETURN 1
	ENDIF
ELSEIF RESULT == 2
	CALL SHOW_ATTACK_ITEM_LIST(ARG:0)
	IF RESULT == 0
		;アイテム使用をキャンセルした場合、最初に戻す
		GOTO START
	ELSE
		;CALL SET_DEFAULT_WAIT(ARG:0)
		RETURN 1
	ENDIF
ELSE
	REUSELASTLINE 入力値が不正です。再入力してください
	GOTO INPUT_LOOP
ENDIF

GOTO START
