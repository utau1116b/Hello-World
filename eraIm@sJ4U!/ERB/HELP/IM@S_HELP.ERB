;iM@sNOTE
;
;

@IMAS_NOTE
;
;
;メインメニュー
DRAWLINE
PRINTL 何を調べますか？
DRAWLINE
PRINTL [1] - PP取得状況
PRINTL [2] - 偶像調教典
;PRINTL [3] - アルバム
PRINTL [100] - ヘルプ終了

INPUT

SELECTCASE RESULT
	CASE 1
		CALL POINT_MENU
	CASE 2
		CALL DICTIONARY_MENU
	CASE 3
		;CALL GARALLY
	CASEELSE
		RETURN
ENDSELECT

RESTART
;アイドルを調教してえられるボーナスポイントについての情報
@POINT_MENU
DRAWLINE
PRINTL どの項目について確認しますか？
DRAWLINE
;SIF ABL:MASTER:Pランク >= 1
;	PRINTFORML [1] プロデュース課題Ⅰ
;SIF ABL:MASTER:Pランク >= 2
;	PRINTFORML [2] プロデュース課題Ⅱ
;SIF ABL:MASTER:Pランク >= 3
;	PRINTFORML [3] プロデュース課題Ⅲ
;SIF ABL:MASTER:Pランク >= 4
;	PRINTFORML [4] プロデュース課題Ⅳ
;SIF ABL:MASTER:Pランク >= 5
;	PRINTFORML [5] プロデュース課題Ⅴ
;SIF ABL:MASTER:Pランク >= 6
;	PRINTFORML [6] プロデュース課題Ⅵ
;SIF ABL:MASTER:Pランク >= 7
;	PRINTFORML [7] プロデュース課題Ⅶ
;SIF ABL:MASTER:Pランク >= 8
;	PRINTFORML [8] プロデュース課題Ⅷ
;SIF ABL:MASTER:Pランク >= 9
;	PRINTFORML [9] プロデュース課題Ⅸ
;SIF ABL:MASTER:Pランク >= 10
;	PRINTFORML [10] プロデュース課題Ⅹ

FOR LOCAL:0, 11, (11 + FLAG:キャラ数)
	SIF CFLAG:(LOCAL:0):勧誘状況 == -1
		PRINTFORML [{LOCAL:0}] %CALLNAME:(LOCAL:0)%
NEXT
PRINTL 
PRINTL [100]やめる
DRAWLINE
$INPUT_LOOP
INPUT
IF RESULT == 100
	RETURN 0
ELSEIF RESULT >= 1 && RESULT <= 10
	TRYCALLFORM PRODUCE_REPORT_{RESULT}
ELSEIF RESULT >= 11 && RESULT < (11 + FLAG:キャラ数)
	TRYCALLFORM IDOL_REPORT(RESULT)
ELSE
	PRINTFORM 不適切な番号{RESULT}が指定されました
	PRINTFORMW 再入力してください
	GOTO INPUT_LOOP
ENDIF
RESTART
