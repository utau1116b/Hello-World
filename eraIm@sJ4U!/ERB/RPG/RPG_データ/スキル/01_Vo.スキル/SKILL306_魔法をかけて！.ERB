;306	魔法をかけて

;スキル説明を表示
;ARG:0	スキルレベル
;RETURN	0:レベルアップ不可能　1:レベルアップ可能
@SHOW_SKILL_EXPLAIN_306(ARG:0)
DRAWLINE
PRINTFORML %ABLNAME:306%
PRINTL スキル説明（アイドル達の語り口）
DRAWLINE
PRINT 時の祝福。
CALL STARMARK_Y
PRINTL 一時増加。SPD上昇。

;ハート軽減チェック
IF ABL:(ARG:0):306 > 3
CALL SHOW_EXPEND_MP(ARG:0, 0)
ELSEIF ABL:(ARG:0):306 > 0
CALL SHOW_EXPEND_MP(ARG:0, 0)
ENDIF
PRINTL 
DRAWLINE


IF ABL:(ARG:0):306 >= 5
	RETURN 0
ELSE
	RETURN 1
ENDIF


;使用可能チェック
;ARG:0	対象キャラ登録番号
@COMABLE_306(ARG:0)
LOCAL:0 = 1
LOCAL:1 = 0

;封印状態かどうか
IF CFLAG:(ARG:0):シール状態
	LOCAL:0 = 0
ENDIF

;消費MP
LOCAL:1 = 43
CALL CHECK_MP(ARG:0, LOCAL:1)
IF RESULT == 0
	LOCAL:0 = -1
ENDIF

RETURN LOCAL:0

;スキル効果を設定
;ARG:0	使用キャラ登録番号
@SELECT_SKILL_TARGET_306(ARG:0)
IF ARG:0 <= 10
	CALL SELECT_TARGET_POSITION_ENEMY(ARG:0, 4)
ELSE
	;CALL SHOW_SKILL_EXPLAIN_306(ARG:0)
	;POSITIONの引数は(0:敵単体　1:敵全体　2:自分　3:味方単体　4:味方全体　5:敵味方全体)
	CALL SELECT_TARGET_POSITION(4)
ENDIF

IF RESULT == 0
	RETURN 0
ELSE
	LOCAL:0 = RESULT
	
	;スキルID
	CFLAG:(ARG:0):現在状態 = 306
	;時間補正
	;CFLAG:(ARG:0):7131 += CFLAG:(ARG:0):7131 / 10
	;対象
	CFLAG:(ARG:0):行動の対象 = LOCAL:0
	;集中フラグ
	;CFLAG:(ARG:0):7133 = 0
	
	RETURN 1
ENDIF

;スキル発動
;ARG:0	使用キャラ
;ARG:1	対象
@USE_SKILL_306(ARG:0, ARG:1)
CALL INIT_USE_SKILL_FLAG
;スキルID
TFLAG:スキルID = 306
;使用キャラ
TFLAG:使用キャラ = ARG:0
;対象キャラ
TFLAG:対象キャラ = ARG:1
;スキル種別
TFLAG:スキル種別 = 0
;属性変化
TFLAG:属性変化 = 0

IF TFLAG:戦闘中
	IF CFLAG:ARG:2 < 100
		TFLAG:変化POW = 30110100
		TFLAG:変化GUA = 30110100
		TFLAG:変化SPD = 30110100
	ELSEIF CFLAG:ARG:2 < 1000
		TFLAG:変化POW = 30120100
		TFLAG:変化GUA = 30120100
		TFLAG:変化SPD = 30110100
	ELSEIF CFLAG:ARG:2 < 3000
		TFLAG:変化POW = 30130100
		TFLAG:変化GUA = 30120100
		TFLAG:変化SPD = 30120100
	ELSEIF CFLAG:ARG:2 < 10000
		TFLAG:変化POW = 40130100
		TFLAG:変化GUA = 40130100
		TFLAG:変化SPD = 40130100
	ELSE
		TFLAG:変化POW = 40140100
		TFLAG:変化GUA = 40140100
		TFLAG:変化SPD = 40140100
	ENDIF
ENDIF
TFLAG:スリップ治療 = 1
TFLAG:スロウ治療 = 1
TFLAG:カース治療 = 1
TFLAG:ストップ治療 = 1
TFLAG:スリープ治療 = 1
TFLAG:フィアー治療 = 1
TFLAG:シール治療 = 1
TFLAG:ブライン治療 = 1
;攻撃属性
;VO1170
;VI1172
;DA1171
;EX1180
IF TFLAG:スキルID >= 301 && TFLAG:スキルID <= 400
	TFLAG:VO属性 = 1
ELSEIF TFLAG:スキルID >= 501 && TFLAG:スキルID <= 600
	TFLAG:DA属性 = 1
ELSEIF TFLAG:スキルID >= 401 && TFLAG:スキルID <= 500
	TFLAG:VI属性 = 1
ELSE
	TFLAG:無属性 = 0
ENDIF


;消費ハート
TFLAG:消費ハート = 43



PRINTFORML %CALLNAME:(ARG:0)%＞＞%SONGNAME:(306-300)%！

CALL COMMON_SKILL_FIX






