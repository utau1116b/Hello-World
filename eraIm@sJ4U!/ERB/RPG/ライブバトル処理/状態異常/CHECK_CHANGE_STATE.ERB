;状態変化チェック
;ARG:0	対象キャラ登録番号
;どう変化させるかなどはTFLAGを見る
@CHECK_CHANGE_STATE(ARG:0)
;LOCAL
;0	LOOP
;1	LOOP
;2	発動率一時保存
;3	ID一時保存
;4	抵抗確率一時保存
;5	残り時間一時保存
;6	何らかの効果を発揮したフラグ（戻り値）

FOR LOCAL:0, 1, 7
	LOCAL:(LOCAL:0) = 0
NEXT

;スリップ～気絶
FOR LOCAL:0, 1041, 1050
	IF TFLAG:(LOCAL:0)
		LOCAL:2 = TFLAG:(LOCAL:0) % 1000
		LOCAL:4 = 100 - CFLAG:(ARG:0):(LOCAL:0 - 21)
		LOCAL:5 = TFLAG:(LOCAL:0) / 1000
		
		LOCAL:2 = LOCAL:2 * LOCAL:4 / 100
		
		IF RAND:100 < LOCAL:2
			IF LOCAL:0 < 1044 || LOCAL:0 == 1049
				;時間経過で治らないものはフラグを立てるだけ
				IF CFLAG:(ARG:0):(LOCAL:0 - 10) == 0
					;新しくこの状態になる場合、メッセージを表示
					CALL SHOW_SET_STATE_MESSAGE(ARG:0, (LOCAL:0 - 1040))
					LOCAL:6 = 1
				ENDIF
				;
				CFLAG:(ARG:0):(LOCAL:0 - 10) = 1
			ELSE
				;他は普通に残り時間を代入
				IF CFLAG:(ARG:0):(LOCAL:0 - 10) < 10 * LOCAL:5
					;効果時間が延長される場合、メッセージを表示＋効果反映
					CALL SHOW_SET_STATE_MESSAGE(ARG:0, (LOCAL:0 - 1040))
					CFLAG:(ARG:0):(LOCAL:0 - 10) = 10 * LOCAL:5
					LOCAL:6 = 1
				ENDIF
			ENDIF
		ENDIF
	ENDIF
NEXT



;スリップ～気絶を治療
FOR LOCAL:0, 1051, 1060
	IF TFLAG:(LOCAL:0) && CFLAG:(ARG:0):(LOCAL:0 - 20)
		CALL SHOW_CURE_STATE_MESSAGE(ARG:0, (LOCAL:0 - 950))
		CALL BMSG_WAIT(2)
		CFLAG:(ARG:0):(LOCAL:0 - 20) = 0
		IF LOCAL:0 == 1059
			CFLAG:(ARG:0):現在状態 = 0
			IF BASE:(ARG:0):HP <= 0
				BASE:(ARG:0):HP = 1
			ENDIF
		ENDIF
		LOCAL:6 = 1
	ENDIF
NEXT

;ステ変化
;2	発動率一時保存
;3	ID一時保存
;4	抵抗確率一時保存
;5	残り時間一時保存
;6	何らかの効果を発揮したフラグ（戻り値）
FOR LOCAL:0, 1060, 1068
	IF TFLAG:(LOCAL:0) > 0
		LOCAL:2 = (TFLAG:(LOCAL:0) % 1000)
		LOCAL:4 = 100 - CFLAG:(ARG:0):能力低下抵抗率
		LOCAL:5 = TFLAG:(LOCAL:0) / 1000
		
		;個別耐性反映
		LOCAL:4 = LOCAL:4 * (100 - CFLAG:(ARG:0):(LOCAL:0 + 250)) / 100
		
		;能力低下でなければ抵抗されない
		IF (LOCAL:5 % 1000) >= 100
			LOCAL:4 = 100
		ENDIF
		
		LOCAL:2 = LOCAL:2 * LOCAL:4 / 100
		IF RAND:100 < LOCAL:2
			;残り時間や変化量に関わらず、強制上書き
			CALL SHOW_SET_STATE_MESSAGE(ARG:0, (LOCAL:0  - 900))
			;値を代入
			CFLAG:(ARG:0):(LOCAL:0 + 70) = LOCAL:5
			LOCAL:6 = 1
		ELSEIF (TFLAG:(LOCAL:0) % 1000) >= 100
			;100%成功の物が失敗になった場合は誤魔化せないのでメッセージを出す
			PRINTFORML %CALLNAME:(ARG:0)%は能力低下に抵抗した！
			CALL BMSG_WAIT(1)
		ENDIF
	ELSEIF TFLAG:(LOCAL:0) < 0 && CFLAG:(ARG:0):(LOCAL:0 + 70)
		;リセットの場合
		CALL SHOW_CURE_STATE_MESSAGE(ARG:0, (LOCAL:0 - 900))
		CFLAG:(ARG:0):(LOCAL:0 + 70) = 0
		LOCAL:6 = 1
	ENDIF
NEXT


;未完成
;加算系ステ変化
FOR LOCAL:0, 1126, 1130
;LOCAL:2	確率
;LOCAL:5	例30130→時間、上昇量
;LOCAL:4	能力低下耐性
;LOCAL:5が130なら実際は130－100で30アップ
	IF TFLAG:(LOCAL:0) > 0
		LOCAL:2 = (TFLAG:(LOCAL:0) % 1000)
		LOCAL:4 = 100 - CFLAG:(ARG:0):能力低下抵抗率
		LOCAL:5 = TFLAG:(LOCAL:0) / 1000
		
		;加算系の耐性はまだ設定していないので、今のところ確実に効く
		;個別耐性反映
		;LOCAL:4 = LOCAL:4 * (100 - CFLAG:(ARG:0):(LOCAL:0 - 200 + 500)) / 100
		
		;能力低下でなければ抵抗されない
		IF (LOCAL:5 % 1000) >= 100
			LOCAL:4 = 100
		ENDIF
		LOCAL:2 = LOCAL:2 * LOCAL:4 / 100
		IF RAND:100 < LOCAL:2
			;値を代入
			;CFLAG:(ARG:0):(LOCAL:0 + 100 - 200) = LOCAL:5
			;POW
			IF TFLAG:加算POW
				CFLAG:(ARG:0):変化加算POW = LOCAL:5
				CALL SHOW_SET_STATE_MESSAGE(ARG:0, 162)
			ENDIF
			;GUA
			IF TFLAG:加算GUA
				CFLAG:(ARG:0):変化加算GUA = LOCAL:5
				CALL SHOW_SET_STATE_MESSAGE(ARG:0, 163)
			ENDIF
			;SPD
			IF TFLAG:加算SPD
				CFLAG:(ARG:0):変化加算SPD = LOCAL:5
				CALL SHOW_SET_STATE_MESSAGE(ARG:0, 166)
			ENDIF
			;HIT
			;IF TFLAG:加算HIT
			;	CFLAG:(ARG:0):変化加算HIT = LOCAL:5
			;	CALL SHOW_SET_STATE_MESSAGE(ARG:0, 165)
			;ENDIF
			LOCAL:6 = 1
		ELSEIF (TFLAG:(LOCAL:0) % 1000) >= 100
			;100%成功の物が失敗になった場合は誤魔化せないのでメッセージを出す
			PRINTFORML %CALLNAME:(ARG:0)%は能力低下に抵抗した！
		ENDIF
	ELSEIF TFLAG:加算POW < 0 && CFLAG:(ARG:0):変化加算POW
		;リセットの場合
		CALL SHOW_CURE_STATE_MESSAGE(ARG:0, 162)
		CFLAG:(ARG:0):変化加算POW = 0
		LOCAL:6 = 1
	ELSEIF TFLAG:加算GUA < 0 && CFLAG:(ARG:0):変化加算GUA
		;リセットの場合
		CALL SHOW_CURE_STATE_MESSAGE(ARG:0, 163)
		CFLAG:(ARG:0):変化加算GUA = 0
		LOCAL:6 = 1
	ELSEIF TFLAG:加算SPD < 0 && CFLAG:(ARG:0):変化加算SPD
		;リセットの場合
		CALL SHOW_CURE_STATE_MESSAGE(ARG:0, 166)
		CFLAG:(ARG:0):変化加算SPD = 0
		LOCAL:6 = 1
	;ELSEIF TFLAG:加算HIT < 0 && CFLAG:(ARG:0):変化加算HIT
		;リセットの場合
	;	CALL SHOW_CURE_STATE_MESSAGE(ARG:0, 165)
	;	CFLAG:(ARG:0):変化加算HIT = 0
	;	LOCAL:6 = 1
	ENDIF
NEXT



;耐性変化
FOR LOCAL:0, 1068, 1081
	IF TFLAG:(LOCAL:0) > 0
		LOCAL:2 = (TFLAG:(LOCAL:0) % 1000)
		LOCAL:5 = TFLAG:(LOCAL:0) / 1000
		
		;とりあえず耐性変化は必ず効くようにする
		IF RAND:100 < LOCAL:2
			;残り時間や変化量に関わらず、強制上書き
			CALL SHOW_SET_STATE_MESSAGE(ARG:0, (LOCAL:0 - 900))
			;値を代入
			CFLAG:(ARG:0):(LOCAL:0 + 70) = LOCAL:5
			LOCAL:6 = 1
		ELSEIF (TFLAG:(LOCAL:0) % 1000) >= 100
			PRINTFORML %CALLNAME:(ARG:0)%は耐性変化に抵抗した！
			CALL BMSG_WAIT(1)
		ENDIF
	ELSEIF TFLAG:(LOCAL:0) < 0 && CFLAG:(ARG:0):(LOCAL:0 + 70)
		;リセットの場合
		CALL SHOW_CURE_STATE_MESSAGE(ARG:0, (LOCAL:0 - 900))
		CFLAG:(ARG:0):(LOCAL:0 + 70) = 0
		LOCAL:6 = 1
	ENDIF
NEXT

;能力低下治療の場合
IF TFLAG:能力低下治療
	FOR LOCAL:0, 1130, 1151
		IF (CFLAG:(ARG:0):(LOCAL:0) % 1000) < 100 && CFLAG:(ARG:0):(LOCAL:0) != 0
			SETCOLOR 0x10EEEE
			CALL SHOW_CURE_STATE_MESSAGE(ARG:0, (LOCAL:0 - 1000 + 32))
			CFLAG:(ARG:0):(LOCAL:0) = 0
			LOCAL:6 = 1
		ENDIF
	NEXT
ENDIF



;強制状態異常変化
IF TFLAG:特殊状態変化
	LOCAL:2 = TFLAG:特殊状態変化 % 1000
	LOCAL:3 = TFLAG:特殊状態変化 / 1000
	LOCAL:5 = TFLAG:特殊状態変化値
	IF RAND:100 < LOCAL:2
		IF CFLAG:(ARG:0):(LOCAL:3) < LOCAL:5
			;残り時間や効果量が上昇する場合のみ更新
			CALL SHOW_SET_STATE_MESSAGE(ARG:0, LOCAL:3 - 1000)
			CFLAG:(ARG:0):(LOCAL:3) = LOCAL:5
			LOCAL:6 = 1
		ELSEIF LOCAL:5 < 0
			;マイナスの効果時間は強制治療
			CALL SHOW_CURE_STATE_MESSAGE(ARG:0, LOCAL:3 - 1000)
			CFLAG:(ARG:0):(LOCAL:3) = 0
			LOCAL:6 = 1
		ENDIF
	ENDIF
	;未実装
	;IF LOCAL:3 == 1120
	;	;変身状態になる＝変身完了なので、服などを元に戻す
	;	CALL HENSHIN_END(ARG:0)
	;ENDIF
ENDIF
	
RETURN LOCAL:6

;状態異常メッセージ表示
;ARG:0	対象キャラID
;ARG:1	状態異常ID
@SHOW_SET_STATE_MESSAGE(ARG:0, ARG:1)
SELECTCASE ARG:1
	CASE 1
		PRINTFORM %CALLNAME:(ARG:0)%＞＞
		CALL STARMARK_Y
		PRINTFORML スリップ！
	CASE 2
		PRINTFORM %CALLNAME:(ARG:0)%＞＞
		PRINTFORML スロウ！行動後WT2倍！
	CASE 3
		PRINTFORML %CALLNAME:(ARG:0)%＞＞カース！与ダメージ1/4反射！
	CASE 4
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ストップ！時が止まる！
	CASE 5
		PRINTFORML %CALLNAME:(ARG:0)%＞＞スリープ！眠り始める！
	CASE 6
		PRINTFORML %CALLNAME:(ARG:0)%＞＞フィアー！恐怖に凍りつく！
	CASE 7
		PRINTFORML %CALLNAME:(ARG:0)%＞＞シール！特技封印！
	CASE 8
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ブライン！HIT低下！
	CASE 9
		PRINTFORML %CALLNAME:(ARG:0)%＞＞気絶！
	CASE 41
		PRINTFORML %CALLNAME:(ARG:0)%＞＞潜在能力を全て引き出した！
	CASE 42
		PRINTFORML %CALLNAME:(ARG:0)%＞＞パーフェクトアピール率がＭＡＸ！
	CASE 43
		PRINTFORML %CALLNAME:(ARG:0)%＞＞！
	CASE 44
		PRINTFORM %CALLNAME:(ARG:0)%＞＞{TFLAG:特殊状態変化値}ターン後に強力なダメージ！
	CASE 45
		PRINTFORML %CALLNAME:(ARG:0)%＞＞DA強化！
	CASE 46
		PRINTFORML %CALLNAME:(ARG:0)%＞＞四分身！
	CASE 47
		PRINTFORML %CALLNAME:(ARG:0)%＞＞アピール回数増加！
	CASE 48
		PRINTFORML %CALLNAME:(ARG:0)%＞＞アピール必中！
	CASE 49
		PRINTFORML %CALLNAME:(ARG:0)%＞＞行動後WT短縮！
	CASE 50
		PRINTFORML %CALLNAME:(ARG:0)%＞＞カウンター状態！
	CASE 51
		PRINTFORML %CALLNAME:(ARG:0)%＞＞VO強化！
	CASE 52
		PRINTFORML %CALLNAME:(ARG:0)%＞＞クリティカル率上昇！
	CASE 53
		PRINTFORML %CALLNAME:(ARG:0)%＞＞全状態異常抵抗！
	CASE 54
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 55
		PRINTFORML %CALLNAME:(ARG:0)%の！
	CASE 56
		PRINTFORML %CALLNAME:(ARG:0)%の！
	CASE 57
		PRINTFORML %CALLNAME:(ARG:0)%の！
	CASE 58
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 59
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 60
		PRINTFORML %CALLNAME:(ARG:0)%＞＞生体時間加速！
	CASE 61
		PRINTFORML %CALLNAME:(ARG:0)%＞＞時の束縛から解放！
	CASE 62
		PRINTFORML %CALLNAME:(ARG:0)%＞＞時が重くのしかかる！
	CASE 63
		PRINTFORML %CALLNAME:(ARG:0)%＞＞被ダメージ上昇！
	CASE 64
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ステージから離脱！
	CASE 65
		PRINTFORML %CALLNAME:(ARG:0)%！
	CASE 66
	;	お前を倒せと轟叫ぶ！！
		PRINTFORML %CALLNAME:(ARG:0)%＞＞闘志が真っ赤に燃える！
	CASE 67
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 68
		PRINTFORML %CALLNAME:(ARG:0)%への！
	CASE 69
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 70
		PRINTFORML %CALLNAME:(ARG:0)%＞＞強力なバリアを展開！
	CASE 71
		PRINTFORML %CALLNAME:(ARG:0)%＞＞バリア展開！
;被ダメ増は表示なし
	CASE 72
		PRINTFORM %CALLNAME:(ARG:0)%＞＞
		CALL STARMARK_Y
		PRINTFORML 自動回復！
	CASE 73
		PRINTFORM %CALLNAME:(ARG:0)%＞＞
		CALL HEARTMARK_P
		PRINTFORML 自動回復！
	CASE 74
		PRINTFORML %CALLNAME:(ARG:0)%＞＞全ダメージを引き受ける！
	CASE 75
		PRINTFORML %CALLNAME:(ARG:0)%＞＞2倍アピール！
	CASE 76
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ガード！
	CASE 77
		PRINTFORML %CALLNAME:(ARG:0)%＞＞！
	CASE 78
		PRINTFORML %CALLNAME:(ARG:0)%の！
	CASE 79
		PRINTFORM %CALLNAME:(ARG:0)%＞＞
		CALL STARMARK_Y
		PRINTFORML 継続減少！
	CASE 80
		PRINTFORML %CALLNAME:(ARG:0)%＞＞VI強化！
	CASE 81
		PRINTFORM %CALLNAME:(ARG:0)%＞＞分身！
	CASE 82
		PRINTFORM %CALLNAME:(ARG:0)%の
	CASE 83
		PRINTFORML %CALLNAME:(ARG:0)%の
	CASE 84
		PRINTFORML 
	CASE 85
		PRINTFORML %CALLNAME:(ARG:0)%は
	CASE 86
		PRINTFORML 
	CASE 87
		PRINTFORML 
	CASE 88
		PRINTFORML 
	CASE 89
		PRINTFORML 
	CASE 90
		PRINTFORM %CALLNAME:(ARG:0)%＞＞消費
		CALL HEARTMARK_P
		PRINTFORML 軽減！
	CASE 91
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 92
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 93
		PRINTFORML %CALLNAME:(ARG:0)%の！
	;PRINTFORML CURE SLIP RECOVER
	;PRINTFORML CURE SLOW
	CASE 160
		PRINTFORM %CALLNAME:(ARG:0)%＞＞最大
		CALL STARMARK_Y
		PRINTFORML 変化！
	CASE 161
		PRINTFORM %CALLNAME:(ARG:0)%＞＞最大
		CALL HEARTMARK_P
		PRINTFORML 変化！
	;文字を極力使用しない表示
	;PRINTFORML ↑↑POW
	CASE 162
		PRINTFORML %CALLNAME:(ARG:0)%＞＞POW変化！
	CASE 163
		PRINTFORML %CALLNAME:(ARG:0)%＞＞GUA変化！
	CASE 164
		PRINTFORML %CALLNAME:(ARG:0)%＞＞DEX変化！
	CASE 165
		PRINTFORML %CALLNAME:(ARG:0)%＞＞HIT変化！
	CASE 166
		PRINTFORML %CALLNAME:(ARG:0)%＞＞SPD変化！
	CASE 167
		PRINTFORML %CALLNAME:(ARG:0)%＞＞LUC変化！
	CASE 168
		PRINTFORML %CALLNAME:(ARG:0)%＞＞VO耐性変化！
	CASE 169
		PRINTFORML %CALLNAME:(ARG:0)%＞＞DA耐性変化！
	CASE 170
		PRINTFORML %CALLNAME:(ARG:0)%＞＞VI耐性変化！
	CASE 171
		PRINTFORML %CALLNAME:(ARG:0)%＞＞EX耐性変化！
	CASE 172
		PRINTFORML %CALLNAME:(ARG:0)%＞＞スリップ耐性変化！
	CASE 173
		PRINTFORML %CALLNAME:(ARG:0)%＞＞スロウ耐性変化！
	CASE 174
		PRINTFORML %CALLNAME:(ARG:0)%＞＞カース耐性変化！
	CASE 175
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ストップ耐性変化！
	CASE 176
		PRINTFORML %CALLNAME:(ARG:0)%＞＞スリープ耐性変化！
	CASE 177
		PRINTFORML %CALLNAME:(ARG:0)%＞＞フィアー耐性変化！
	CASE 178
		PRINTFORML %CALLNAME:(ARG:0)%＞＞シール耐性変化！
	CASE 179
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ブライン耐性変化！
	CASE 180
		PRINTFORML %CALLNAME:(ARG:0)%＞＞気絶耐性変化！
	CASEELSE
		PRINTFORMW エラー！不正な状態異常ID{ARG:1}が指定されました(SHOW_SET_STATE_MESSAGE)
ENDSELECT
RESETCOLOR

;状態異常回復メッセージ表示
;ARG:0	対象キャラID
;ARG:1	状態異常ID
@SHOW_CURE_STATE_MESSAGE(ARG:0, ARG:1)
SELECTCASE ARG:1
	CASE 31
		PRINTFORML %CALLNAME:(ARG:0)%＞＞スリップ回復！
	CASE 32
		PRINTFORML %CALLNAME:(ARG:0)%＞＞スロウ状態回復！
	CASE 33
		PRINTFORML %CALLNAME:(ARG:0)%＞＞カース状態回復！
	CASE 34
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ストップ状態回復！
	CASE 35
		PRINTFORML %CALLNAME:(ARG:0)%＞＞スリープ状態回復！
	CASE 36
		PRINTFORML %CALLNAME:(ARG:0)%＞＞フィアー状態回復！
	CASE 37
		PRINTFORML %CALLNAME:(ARG:0)%＞＞シール状態回復！
	CASE 38
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ブライン状態回復！
	CASE 39
		PRINTFORML %CALLNAME:(ARG:0)%＞＞気絶状態回復！
	CASE 41
		PRINTFORML %CALLNAME:(ARG:0)%＞＞能力通常化！
	CASE 42
		PRINTFORML %CALLNAME:(ARG:0)%＞＞アピール率通常化！
	CASE 43
		PRINTFORML %CALLNAME:(ARG:0)%＞＞！
	CASE 44
		PRINTFORM %CALLNAME:(ARG:0)%＞＞！
	CASE 45
		PRINTFORML %CALLNAME:(ARG:0)%＞＞DA強化解除！
	CASE 46
		PRINTFORML %CALLNAME:(ARG:0)%＞＞四分身終了！
	CASE 47
		PRINTFORML %CALLNAME:(ARG:0)%＞＞アピール回数増加解除！
	CASE 48
		PRINTFORML %CALLNAME:(ARG:0)%＞＞アピール必中解除！
	CASE 49
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 50
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 51
		PRINTFORML %CALLNAME:(ARG:0)%＞＞VO強化解化！
	CASE 52
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 53
		PRINTFORML %CALLNAME:(ARG:0)%＞＞全状態異常抵抗解除！
	CASE 54
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 55
		PRINTFORML %CALLNAME:(ARG:0)%の！
	CASE 56
		PRINTFORML %CALLNAME:(ARG:0)%の！
	CASE 57
		PRINTFORML %CALLNAME:(ARG:0)%の！
	CASE 58
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 59
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 60
		PRINTFORML %CALLNAME:(ARG:0)%＞＞連続行動解除！
	CASE 61
		PRINTFORML %CALLNAME:(ARG:0)%＞＞時の祝福解除！
	CASE 62
		PRINTFORML %CALLNAME:(ARG:0)%＞＞時の束縛解除！
	CASE 63
		PRINTFORML %CALLNAME:(ARG:0)%＞＞被ダメージ増加解除！
	CASE 64
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ステージ復帰！
	CASE 65
		PRINTFORML %CALLNAME:(ARG:0)%！
	CASE 66
	;	お前を倒せと轟叫ぶ！！
		PRINTFORML %CALLNAME:(ARG:0)%＞＞不屈の闘志消沈！
	CASE 67
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 68
		PRINTFORML %CALLNAME:(ARG:0)%への！
	CASE 69
		PRINTFORML %CALLNAME:(ARG:0)%は！
	CASE 70
		PRINTFORML %CALLNAME:(ARG:0)%＞＞アブソリュート・フィールド解除！
	CASE 71
		PRINTFORML %CALLNAME:(ARG:0)%＞＞バリア解除！
;被ダメ増は表示なし
	CASE 72
		PRINTFORM %CALLNAME:(ARG:0)%＞＞
		CALL STARMARK_Y
		PRINTFORML 自動回復終了！
	CASE 73
		PRINTFORM %CALLNAME:(ARG:0)%＞＞
		CALL HEARTMARK_P
		PRINTFORML 自動回復終了！
	CASE 74
		PRINTFORML %CALLNAME:(ARG:0)%は
	CASE 75
		PRINTFORML %CALLNAME:(ARG:0)%＞＞バーストアピール解除！
	CASE 76
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ガード解除！
	CASE 77
		PRINTFORML %CALLNAME:(ARG:0)%＞＞！
	CASE 78
		PRINTFORML %CALLNAME:(ARG:0)%の！
	CASE 79
		PRINTFORM %CALLNAME:(ARG:0)%＞＞
		CALL STARMARK_Y
		PRINTFORML 継続減少終了！
	CASE 80
		PRINTFORML %CALLNAME:(ARG:0)%＞＞VI強化解除！
	CASE 81
		PRINTFORM %CALLNAME:(ARG:0)%＞＞分身終了！
	CASE 82
		PRINTFORM %CALLNAME:(ARG:0)%の
	CASE 83
		PRINTFORML %CALLNAME:(ARG:0)%の
	CASE 84
		PRINTFORML 
	CASE 85
		PRINTFORML %CALLNAME:(ARG:0)%は
	CASE 86
		PRINTFORML 
	CASE 87
		PRINTFORML 
	CASE 88
		PRINTFORML 
	CASE 89
		PRINTFORML 
	CASE 90
		PRINTFORM %CALLNAME:(ARG:0)%＞＞消費
		CALL HEARTMARK_P
		PRINTFORML 軽減終了！
	CASE 130
		PRINTFORM %CALLNAME:(ARG:0)%＞＞最大
		CALL STARMARK_Y
		PRINTFORML 変化解除！
	CASE 131
		PRINTFORM %CALLNAME:(ARG:0)%＞＞最大
		CALL HEARTMARK_P
		PRINTFORML 変化解除！
	;文字を極力使用しない表示
	;PRINTFORML ↑↑POW
	CASE 132
		PRINTFORML %CALLNAME:(ARG:0)%＞＞POW変化解除！
	CASE 133
		PRINTFORML %CALLNAME:(ARG:0)%＞＞GUA変化解除！
	CASE 134
		PRINTFORML %CALLNAME:(ARG:0)%＞＞DEX変化解除！
	CASE 135
		PRINTFORML %CALLNAME:(ARG:0)%＞＞HIT変化解除！
	CASE 136
		PRINTFORML %CALLNAME:(ARG:0)%＞＞SPD変化解除！
	CASE 137
		PRINTFORML %CALLNAME:(ARG:0)%＞＞LUC変化解除！
	CASE 138
		PRINTFORML %CALLNAME:(ARG:0)%＞＞VO耐性変化解除！
	CASE 139
		PRINTFORML %CALLNAME:(ARG:0)%＞＞DA耐性変化解除！
	CASE 140
		PRINTFORML %CALLNAME:(ARG:0)%＞＞VI耐性変化解除！
	CASE 141
		PRINTFORML %CALLNAME:(ARG:0)%＞＞EX耐性変化解除！
	CASE 142
		PRINTFORML %CALLNAME:(ARG:0)%＞＞スリップ耐性変化解除！
	CASE 143
		PRINTFORML %CALLNAME:(ARG:0)%＞＞スロウ耐性変化解除！
	CASE 144
		PRINTFORML %CALLNAME:(ARG:0)%＞＞カース耐性変化解除！
	CASE 145
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ストップ耐性変化解除！
	CASE 146
		PRINTFORML %CALLNAME:(ARG:0)%＞＞スリープ耐性変化解除！
	CASE 147
		PRINTFORML %CALLNAME:(ARG:0)%＞＞フィアー耐性変化解除！
	CASE 148
		PRINTFORML %CALLNAME:(ARG:0)%＞＞シール耐性変化解除！
	CASE 149
		PRINTFORML %CALLNAME:(ARG:0)%＞＞ブライン耐性変化解除！
	CASE 150
		PRINTFORML %CALLNAME:(ARG:0)%＞＞気絶耐性変化解除！
	CASEELSE
		PRINTFORMW エラー！不正な状態異常ID{ARG:1}が指定されました(SHOW_CURE_STATE_MESSAGE)
ENDSELECT
RESETCOLOR
CALL BMSG_WAIT(2)

