@FAINT_CHECK

FOR LOCAL:0, 1080, 1085
	IF FLAG:(LOCAL:0) > 0
		IF CFLAG:(FLAG:(LOCAL:0)):気絶状態 == 0
			IF BASE:(FLAG:(LOCAL:0)):HP <= 0
				BASE:(FLAG:(LOCAL:0)):HP = 0
				CFLAG:(FLAG:(LOCAL:0)):気絶状態 = 1
				PRINTFORMW %CALLNAME:(FLAG:(LOCAL:0))%は気絶した！
			ENDIF
		ENDIF
	ENDIF
NEXT

FOR LOCAL:0, 1, 11
	IF CFLAG:(LOCAL:0):気絶状態 == 0
		IF BASE:(LOCAL:0):HP <= 0
			BASE:(LOCAL:0 ):HP = 0
			CFLAG:(LOCAL:0):気絶状態 = 1
			PRINTFORMW %CALLNAME:(LOCAL:0)%は気絶した！
		ENDIF
	ENDIF
NEXT
