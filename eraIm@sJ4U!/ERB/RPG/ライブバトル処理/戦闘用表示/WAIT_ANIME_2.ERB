@WAIT_ANIME_2

FOR LOCAL:0, 3101, 3116
	FLAG:(LOCAL:0) = 0
NEXT

;敵タイムカード
FOR LOCAL:0, 1, 11
	IF TFLAG:(LOCAL:0 + 1000) > 0 && CFLAG:(LOCAL:0):気絶状態 != 1
		FLAG:(3100 + (LOCAL:0)) = LOCAL:0
	ENDIF
NEXT
;味方タイムカード
FOR LOCAL:0, 1080, 1085
	IF FLAG:(LOCAL:0) > 0 && CFLAG:(FLAG:(LOCAL:0)):気絶状態 != 1
		FLAG:((LOCAL:0) + 2031) = FLAG:(LOCAL:0)
	ENDIF
NEXT

FOR LOCAL:0, 3101, 3119
	IF FLAG:(LOCAL:0) > 0
	ENDIF
NEXT
	
;WAIT

FOR LOCAL:0, 0, 23
	A:(LOCAL:0) = 0
NEXT

GETFONT
CHKFONT "Times New Roman"
SIF RESULT
	SETFONT "Times New Roman"
PRINTFORM  W T
SETFONT RESULTS
PRINT 　[
FOR LOCAL:0, 1, 66
	FOR LOCAL:1, 3101, 3116
		;もしも待機時間が3*LOCALより長ければ
		IF CFLAG:(FLAG:(LOCAL:1)):残り待機時間 > (3 * (LOCAL:0 - 1)) && CFLAG:(FLAG:(LOCAL:1)):残り待機時間 <= 3 * LOCAL:0
			IF FLAG:(LOCAL:1) != A:0 && FLAG:(LOCAL:1) != A:1 && FLAG:(LOCAL:1) != A:2 && FLAG:(LOCAL:1) != A:3 && FLAG:(LOCAL:1) != A:4 && FLAG:(LOCAL:1) != A:5 && FLAG:(LOCAL:1) != A:6 && FLAG:(LOCAL:1) != A:7 && FLAG:(LOCAL:1) != A:8 && FLAG:(LOCAL:1) != A:9 && FLAG:(LOCAL:1) != A:10 && FLAG:(LOCAL:1) != A:11 && FLAG:(LOCAL:1) != A:12 && FLAG:(LOCAL:1) != A:13 && FLAG:(LOCAL:1) != A:14 && FLAG:(LOCAL:1) != A:15
				CALL PRINT_BATTLE_NAME(FLAG:(LOCAL:1))
				;空き番号にキャラ番号を入れる
				FOR LOCAL:6, 0, 16
					IF A:(LOCAL:6) == 0
						A:(LOCAL:6) = (FLAG:(LOCAL:1))
					ENDIF
				NEXT
			ENDIF
		ENDIF
	NEXT
	;タイムカード表示
	FOR LOCAL:1, 1256, 1259
		IF TFLAG:(LOCAL:1)
			IF TFLAG:(LOCAL:1) > (3 * (LOCAL:0 - 1)) && TFLAG:(LOCAL:1) <= 3 * LOCAL:0
				IF LOCAL:1 != A:16 && LOCAL:1 != A:17 && LOCAL:1 != A:18
					CALL PRINT_SKILL_NAME(LOCAL:1)
					;空き番号にキャラ番号を入れる
					FOR LOCAL:6, 16, 19
						IF A:(LOCAL:6) == 0
							A:(LOCAL:6) = LOCAL:1
						ENDIF
					NEXT
				ENDIF
			ENDIF
		ENDIF
	NEXT
	;舞分身表示
	FOR LOCAL:1, 1263, 1266
		IF TFLAG:(LOCAL:1)
			IF TFLAG:(LOCAL:1) > (3 * (LOCAL:0 - 1)) && TFLAG:(LOCAL:1) <= 3 * LOCAL:0
				IF LOCAL:1 != A:19 && LOCAL:1 != A:20 && LOCAL:1 != A:21
					CALL PRINT_SKILL_NAME(LOCAL:1)
					;空き番号にキャラ番号を入れる
					FOR LOCAL:6, 19, 22
						IF A:(LOCAL:6) == 0
							A:(LOCAL:6) = LOCAL:1
						ENDIF
					NEXT
				ENDIF
			ENDIF
		ENDIF
	NEXT
	PRINTFORM -
NEXT
;PRINTFORM %LOCALS%
PRINTL ] 
;WAIT


@PRINT_BATTLE_NAME(ARG:0)
IF ARG:0 >= 11
	CALL SET_COLOR(ARG:0)
	PRINTFORM %CSTR:(ARG:0):21%
	RESETCOLOR
ELSE
	SETCOLOR 0xEE1010
	SELECTCASE ARG:0
		CASE 1
			PRINTFORM ①
		CASE 2
			PRINTFORM ②
		CASE 3
			PRINTFORM ③
		CASE 4	
			PRINTFORM ④
		CASE 5
			PRINTFORM ⑤
		CASE 6
			PRINTFORM ⑥
		CASE 7
			PRINTFORM ⑦
		CASE 8
			PRINTFORM ⑧
		CASE 9
			PRINTFORM ⑨
		CASE 10
			PRINTFORM ⑩
		CASEELSE
		
	ENDSELECT
	RESETCOLOR
ENDIF
@PRINT_SKILL_NAME(ARG:0)
;ARG:0	タイムカード番号
SELECTCASE ARG:0
	CASE 1256
		PRINTFORM ★
	CASE 1257
		PRINTFORM ★
	CASE 1258
		PRINTFORM ★
	CASE 1263 TO 1265
		PRINTFORM 舞
ENDSELECT

	
