

;敵ステータス簡易表示
@SHOW_ENEMY_SIMPLE
VARSET LOCAL
PRINTL ＥＮＥＭＹ
ALIGNMENT CENTER
FOR LOCAL, 6, 11
	IF TFLAG:(LOCAL + 1000) > 0 && !CFLAG:LOCAL:気絶状態
		LOCAL:1 = 1
	ELSE
		SETCOLOR COLOR("light-gray")
		LOCAL:1 = 0
	ENDIF
	PRINTPLAINFORM [{LOCAL, 2}] \@ LOCAL:1 ? %CALLNAME:(LOCAL:0),16,LEFT% # %"EMPTY",16,LEFT% \@
	PRINT 　
	RESETCOLOR
NEXT
PRINTL 
FOR LOCAL, 6, 11
	IF TFLAG:(LOCAL + 1000) > 0 && !CFLAG:LOCAL:気絶状態
		CALL BAR_HP, LOCAL, 0, 1
	ELSE
		CALL BAR_HP, , 1, 1
	ENDIF
	PRINT 　
NEXT
PRINTL 
PRINTL 
FOR LOCAL, 1, 6
	IF TFLAG:(LOCAL + 1000) > 0 && !CFLAG:LOCAL:気絶状態
		LOCAL:1 = 1
	ELSE
		SETCOLOR COLOR("light-gray")
		LOCAL:1 = 0
	ENDIF
	PRINTPLAINFORM [{LOCAL, 2}] \@ LOCAL:1 ? %CALLNAME:(LOCAL:0),16,LEFT% # %"EMPTY",16,LEFT% \@
	PRINT 　
	RESETCOLOR
NEXT
PRINTL 
FOR LOCAL, 1, 6
	IF TFLAG:(LOCAL + 1000) > 0 && !CFLAG:LOCAL:気絶状態
		CALL BAR_HP, LOCAL, 0, 1
	ELSE
		CALL BAR_HP, , 1, 1
	ENDIF
	PRINT 　
NEXT
PRINTL 
PRINTL 
ALIGNMENT LEFT
;戦闘中味方状況表示
@SHOW_PT_SIMPLE_BATTLE
;FOR LOCAL:0, 1080, 1085
;	IF FLAG:(LOCAL:0) > 0
;	;付け加え
;		IF CFLAG:(FLAG:(LOCAL:0)):気絶状態
;			SETCOLOR 0xEE1010
;		ENDIF
;		PRINTFORM %CALLNAME:(FLAG:(LOCAL:0)),11,LEFT% 
;		RESETCOLOR
;		PRINTFORM Lv{BASE:(FLAG:(LOCAL:0)):戦闘レベル,2} 
;		
;		CALL PRINT_HPMPEP(FLAG:(LOCAL:0), 0)
;		CALL PRINT_HPMPEP(FLAG:(LOCAL:0), 1)
;		CALL PRINT_HPMPEP(FLAG:(LOCAL:0), 2)
;		
;		PRINT  
;		IF CFLAG:(FLAG:(LOCAL:0)):現在状態 == 0
;			PRINTFORM 待機残り　：{CFLAG:(FLAG:(LOCAL:0)):残り待機時間, 4} [
;			IF CFLAG:(FLAG:(LOCAL:0)):残り待機時間 > 99
;				SETCOLOR 0xEE6060
;			ENDIF
;			FOR LOCAL:1, 0, 33
;				IF CFLAG:(FLAG:(LOCAL:0)):残り待機時間 > (3 * LOCAL:1)
;					PRINT -
;				ENDIF
;			NEXT
;			RESETCOLOR
;			PRINT ] 
;		ELSEIF CFLAG:(FLAG:(LOCAL:0)):現在状態 < 0
;			PRINTFORM 行動不能          
;		ELSE
;			
;		ENDIF
;		CALL SHOW_BAD_STATUS(FLAG:(LOCAL:0))
;		PRINTL
;	ENDIF
;NEXT

