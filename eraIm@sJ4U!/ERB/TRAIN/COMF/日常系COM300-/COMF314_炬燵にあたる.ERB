;-------------------------------------------------
;炬燵にあたる
;日常系コマンド　レベル1
;-------------------------------------------------
@COM314
;着脱の処理
TEQUIP:炬燵 = !TEQUIP:炬燵
;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------
;行為者
TCVAR:116 = PLAYER
;にとりor夢美が居ると熱源が電気式が動くようになります(普通は練炭、木炭、豆炭等)
;修理したものを使ってもらえたので効果アップ
;……とのことだが、アイマスの世界は現代なので、最初から電気式という事で
TFLAG:成功判定 = 1
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、気力
DOWNBASE:体力 += 30
DOWNBASE:気力 += 50

;炬燵を出す
SIF TEQUIP:炬燵
	;固定で獲得するソース
	SOURCE:歓楽 = 50

SOURCE:受動 = 100
SOURCE:征服 = 100
;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;好感度ランダム変化(雪、吹雪、細氷だと+3)
;A:1 = RAND:10 + 5 + (TIME:5 >= 5 && TIME:5 <= 8 && TIME:5 != 7) * 3
;好感度が30以下だと好感度が下がる(一緒に入るなって事です)
IF CFLAG:好感度 <= 30
	;A:1 -= 9
	SOURCE:反感 += 1
ENDIF

;-------------------------------------------------
;汚れの処理
;-------------------------------------------------

;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;同性愛　レズなら戻り値1ゲイなら戻り値2
;IF HETEROSEX(TARGET,PLAYER)
;	EXP:(39 + HETEROSEX(TARGET,PLAYER)) += 3
;	EXP:PLAYER:(39 + HETEROSEX(TARGET,PLAYER)) += 3
;ENDIF

RETURN 1
