
@MAKE_CHARA_LOOKS
VARSET LOCAL
PRINTFORML äOå©Çê›íËÇµÇ‹Ç∑
FOR LOCAL, 121, 138
	SIF !STRLENS(CFLAGNAME:LOCAL)
		CONTINUE
	PRINTFORM [{LOCAL}] %CFLAGNAME:LOCAL, 16, LEFT%Åi
	SELECTCASE LOCAL
		CASE 121
			PRINTFORM %HAIRCOLOR(1)%
		CASE 122
			PRINTFORM %HAIRLENGTH(1)%
		CASE 123
			PRINTFORM %HAIRSTRETCH(1)%
		CASE 124
			PRINTFORM %HAIRSTYLE(1)%
		CASE 125
			PRINTFORM %HAIROPTION(1)%
		CASE 126
			PRINTFORM %HAIROPTION(1,,1)%
		CASE 127
			PRINTFORM %EYECOLOR(1)%
		CASE 128
			PRINTFORM %EYESTYLE(1)%
		CASE 129
			PRINTFORM %SKINCOLOR(1)%
		CASE 130
			PRINTFORM %PERSONALITY(1)%
		CASE 131
			PRINTFORM %JOBNAME(1)%
		CASE 132
			PRINTFORM %JOBNAME(1,,1)%
		CASE 133 TO 137
			PRINTFORM %FLAVOR(1,,LOCAL-132)%
	ENDSELECT
	PRINTFORML Åj
NEXT
PRINTL 
PRINTFORML [0]ñﬂÇÈ
INPUT
LOCAL:1 = RESULT
SELECTCASE RESULT
	CASE 0
		RETURN
	CASE 121 TO 137
		PRINTFORML Ç«ÇÍÇïtÇØÇ‹Ç∑Ç©ÅH
		FOR LOCAL, 1, 100
			CFLAG:1:(LOCAL:1) = LOCAL
			;SELECTCASE LOCAL:1
					SELECTCASE LOCAL:1
						CASE 121
							SIF !STRLENS(@"%HAIRCOLOR(1)%")
								CONTINUE
							SIF HAIRCOLOR(1) == "Å|Å|Å|"
								CONTINUE
							PRINTFORML [{LOCAL}]%HAIRCOLOR(1)%
						CASE 122
							SIF HAIRLENGTH(1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%HAIRLENGTH(1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%HAIRLENGTH(1)%
						CASE 123
							SIF HAIRSTRETCH(1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%HAIRSTRETCH(1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%HAIRSTRETCH(1)%
						CASE 124
							SIF HAIRSTYLE(1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%HAIRSTYLE(1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%HAIRSTYLE(1)%
						CASE 125
							SIF HAIROPTION(1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%HAIROPTION(1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%HAIROPTION(1)%
						CASE 126
							SIF HAIROPTION(1,,1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%HAIROPTION(1,,1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%HAIROPTION(1,,1)%
						CASE 127
							SIF EYECOLOR(1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%EYECOLOR(1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%EYECOLOR(1)%
						CASE 128
							SIF EYESTYLE(1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%EYESTYLE(1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%EYESTYLE(1)%
						CASE 129
							SIF SKINCOLOR(1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%SKINCOLOR(1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%SKINCOLOR(1)%
						CASE 130
							SIF PERSONALITY(1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%PERSONALITY(1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%PERSONALITY(1)%
						CASE 131
							SIF JOBNAME(1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%JOBNAME(1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%JOBNAME(1)%
						CASE 132
							SIF JOBNAME(1,,1) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%JOBNAME(1,,1)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%JOBNAME(1,,1)%
						CASE 133 TO 137
							SIF FLAVOR(1,,LOCAL:1-132) == "Å|Å|Å|"
								CONTINUE
							SIF !STRLENS(@"%FLAVOR(1,,LOCAL:1-132)%")
								CONTINUE
							PRINTFORML [{LOCAL}]%FLAVOR(1,,LOCAL:1-132)%
					ENDSELECT
			;ENDSELECT
		NEXT
		PRINTFORML [0]ñﬂÇÈ
		INPUT
		CFLAG:1:(LOCAL:1) = RESULT
ENDSELECT
RESTART
